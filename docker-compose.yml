services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      PORT: 3000
      CORS_ORIGIN: http://localhost

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
